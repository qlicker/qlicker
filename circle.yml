dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - npm install

checkout:
  post:
    - git submodule update --init

test:
  override:
    - : # noop, temp disable tests on ci

deployment:
  staging:
    branch: staging
    commands:
      - ./deploy.sh
