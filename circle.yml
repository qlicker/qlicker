dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - npm install

checkout:
  post:
    - git submodule update --init

deployment:
  staging:
    branch: master
    commands:
      - ./deploy.sh

test:
  post:
    - bash <(curl -s https://codecov.io/bash) -t bcef8026