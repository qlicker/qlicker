dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - npm install

checkout:
  post:
    - git submodule update --init

test:
  override:
    - npm run test:unit-coverage

deployment:
  staging:
    branch: staging
    commands:
      - ./deploy.sh
